#include <utility>
#include "Spell.h"

/*
offensive_spell::offensive_spell(std::string name, int r, int c,
                                 int s_min, int s_max):
    _name(std::move(name)),
    rank(r),
    mana_cost(c),
    spell_damage_min(s_min),
    spell_damage_max(s_max){}

std::string offensive_spell::get_spell_name() {
    return std::string("offensive_spell");
}

buff_spell::buff_spell(std::string name, int r, int c,
                       std::string s, int m):
    _name(std::move(name)),
    rank(r),
    mana_cost(c),
    modified_stat(std::move(s)),
    stat_modifier(m){}

std::string buff_spell::get_spell_name() {
    return std::string("buff_spell");
}
*/